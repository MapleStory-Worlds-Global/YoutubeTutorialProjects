@BTNode
script CompareBlackboardValues_Int extends DecoratorNode

	property string LeftKey = ""

	property string RightKey = ""

	property integer Operator = 0

	method any OnBehave(number delta)
		local leftValue = self.ParentAI.BlackBoard:GetInt(self.LeftKey)
		local rightValue = self.ParentAI.BlackBoard:GetInt(self.RightKey)
		if _Comparator:Compare(_ValueType.Int, self.Operator, leftValue, rightValue) then
			return self:ChildBehave(delta)
		end
		
		return BehaviourTreeStatus.Failure
	end

end