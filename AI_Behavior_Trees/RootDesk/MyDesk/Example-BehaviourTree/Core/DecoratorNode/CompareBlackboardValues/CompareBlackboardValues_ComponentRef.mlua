@BTNode
script CompareBlackboardValues_ComponentRef extends DecoratorNode

	property string LeftKey = ""

	property string RightKey = ""

	property integer Operator = 0

	method any OnBehave(number delta)
		local leftValue = self.ParentAI.BlackBoard:GetComponentRef(self.LeftKey)
		local rightValue = self.ParentAI.BlackBoard:GetComponentRef(self.RightKey)
		if _Comparator:Compare(_ValueType.ComponentRef, self.Operator, leftValue, rightValue) then
			return self:ChildBehave(delta)
		end
		
		return BehaviourTreeStatus.Failure
	end

end