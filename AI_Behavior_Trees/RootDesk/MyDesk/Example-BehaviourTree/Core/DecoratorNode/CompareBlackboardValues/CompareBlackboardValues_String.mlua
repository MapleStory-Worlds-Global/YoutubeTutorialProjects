@BTNode
script CompareBlackboardValues_String extends DecoratorNode

	property string LeftKey = ""

	property string RightKey = ""

	property integer Operator = 0

	method any OnBehave(number delta)
		local leftValue = self.ParentAI.BlackBoard:GetString(self.LeftKey)
		local rightValue = self.ParentAI.BlackBoard:GetString(self.RightKey)
		if _Comparator:Compare(_ValueType.String, self.Operator, leftValue, rightValue) then
			return self:ChildBehave(delta)
		end
		
		return BehaviourTreeStatus.Failure
	end

end